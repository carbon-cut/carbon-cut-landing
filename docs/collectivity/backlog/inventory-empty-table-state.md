# Inventory Empty Table State

Inventory tables with no rows currently render an awkward empty grid.

Add a consistent empty state for row-based inventory tables: retain the section title and description, explain that no rows have been added, and provide the primary add-row action. Apply it without changing the existing populated-table layout.
