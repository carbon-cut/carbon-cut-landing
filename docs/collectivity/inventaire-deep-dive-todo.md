# Inventaire Deep-Dive TODO

Remaining checklist for the `inventaire` deep-dive.

Purpose:

- keep only the unresolved questions in one place
- know what still blocks the final route contract
- avoid reopening topics already locked in `inventaire-input-inventory.md`

## Exit Condition

We are ready for the `inventaire` route contract only when the items below are answered well enough.

## 1. Minimum Usable Inventory

Answer:

- what the minimum required dataset set is for a first usable inventory result
- what can remain missing
- what blocks progress completely
- what can be estimated or deferred

## 2. Quality And Missing Data

Answer:

- how missing data is represented
- how estimated or proxy data is represented
- what notes or clarification are required
- what validation means at dataset level
- what completeness means operationally

## 3. Import Vs Manual Entry

Answer:

- which source families are better by import
- which source families are realistic for manual entry
- which families require both
- whether files are first-scope or later-scope

## 4. Route-Contract Readiness

Before writing the final `inventaire` route contract, confirm:

- the remaining unresolved points from `inventaire-input-inventory.md`
- the completeness model
- the import/manual split
- the minimum usable dataset set

## Working Rule

Do not infer unresolved answers.

If a question is still unclear, keep it open until discussed explicitly.
