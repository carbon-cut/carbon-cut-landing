### Goal

Create the first calculation service skeleton aligned with the architecture.

### Acceptance criteria

- [ ] Calculation service module/folder exists
- [ ] Service entry point exists
- [ ] Service accepts the current `Project` / `Inventory` context
- [ ] Service uses the parameter resolution service before formula execution
- [ ] Formula execution is separated from direct database access
- [ ] Service can produce a `CalculationRun`-shaped output and a `CalculationResult`-shaped output
- [ ] A dummy end-to-end calculation can run through the service

effort: M
