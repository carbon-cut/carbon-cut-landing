### Goal

Implement the transport calculation slice.

### Acceptance criteria

- [ ] Transport formulas are implemented for `publicTransport`
- [ ] Transport formulas are implemented for `airTransport`
- [ ] Transport formulas are implemented for `port`
- [ ] Transport formulas are implemented for `territoryVehicles`
- [ ] Transport calculation reads from the saved inventory input shape
- [ ] Transport calculation uses resolved parameter entries from the run preload
- [ ] Transport output is written under the `transport` branch of the canonical emissions payload
- [ ] At least one transport sample can be manually checked against a known reference

effort: M
